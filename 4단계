1. 최소, 최대
A = int(input())
B = list(map(int, input().split()))

print(min(B), max(B))



2. 최댓값
import sys
input = sys.stdin.readline
array = []
Max_Num = 0

for i in range(1,10,1):
    A = int(input())
    array.append(A)

Max_Num = max(array)
print(Max_Num)
print(array.index(Max_Num)+1)
